import MessageContainer from "@/components/MessageContainer";

const Home = () => {
  return (
    <section className="w-full h-[calc(100vh-80px)]">
      <MessageContainer />
    </section>
  );
};

export default Home;
